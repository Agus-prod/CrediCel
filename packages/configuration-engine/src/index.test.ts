import { describe, expect, it } from "vitest";
import { AmbiguousConfigurationError, resolveConfiguration, type Candidate } from "./index.js";
const now=new Date("2026-01-01T00:00:00Z"); const base: Candidate={id:"global",key:"down_payment",value:20,scopeType:"organization",scopeId:"org",priority:1,specificity:1,version:1,versionId:"v1",effectiveFrom:new Date("2025-01-01"),effectiveUntil:null,status:"active"};
describe("resolveConfiguration",()=>{it("elige la regla más prioritaria y explica",()=>{const apple={...base,id:"apple",value:30,scopeType:"brand" as const,scopeId:"apple",priority:10,specificity:3};const result=resolveConfiguration([base,apple],{key:"down_payment",at:now,scopes:{organization:["org"],brand:["apple"]}});expect(result.value).toBe(30);expect(result.evaluatedRules).toHaveLength(2);});it("rechaza empates ambiguos",()=>{expect(()=>resolveConfiguration([base,{...base,id:"other"}],{key:"down_payment",at:now,scopes:{organization:["org"]}})).toThrow(AmbiguousConfigurationError);});});
