import { AppShell } from "@/components/app-shell";
import { createServerSupabase } from "@/lib/supabase/server";
import { submitCreditApplication } from "./actions";
const relatedName=(value:unknown)=>Array.isArray(value)?(value[0] as {name?:string}|undefined)?.name:(value as {name?:string}|null)?.name;
export default async function NewSale({searchParams}:{readonly searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const supabase=await createServerSupabase();const {data:{user}}=await supabase.auth.getUser();const [{data:access},{data:inventory}]=await Promise.all([supabase.from("user_branch_access").select("branch_id,branches(name)").eq("profile_id",user?.id??""),supabase.from("inventory_units").select("id,imei_1,color,storage_capacity,cash_price,product_models(name),product_brands(name)").eq("status","available").order("created_at")]);return <AppShell><section className="section"><div className="toolbar"><div><div className="eyebrow">Venta financiada</div><h1>Nueva solicitud de crédito</h1><p className="muted">Registra al cliente dentro de la venta y envía el caso al analista.</p></div></div>{error&&<div className="error">{error}</div>}<form action={submitCreditApplication} className="workspace-stack"><div className="card form-card"><div className="form-title"><div><h2>1. Cliente</h2><p className="muted">Identidad y contacto del solicitante.</p></div></div><div className="form">{[["dni","DNI","text"],["phone","Teléfono","tel"],["first_name","Nombres","text"],["last_name","Apellidos","text"],["email","Correo","email"]].map(([name,label,type])=><div className="field" key={name}><label htmlFor={name}>{label}</label><input id={name} name={name} type={type} required={name!=="email"}/></div>)}</div></div><div className="card form-card"><div className="form-title"><div><h2>2. Dispositivo y condiciones</h2><p className="muted">Solo aparecen dispositivos disponibles en tus tiendas.</p></div></div><div className="form"><div className="field"><label htmlFor="branch_id">Tienda</label><select id="branch_id" name="branch_id" required><option value="">Selecciona una tienda</option>{(access??[]).map(row=><option value={row.branch_id} key={row.branch_id}>{relatedName(row.branches)}</option>)}</select></div><div className="field"><label htmlFor="inventory_unit_id">Dispositivo disponible</label><select id="inventory_unit_id" name="inventory_unit_id" required><option value="">Selecciona un dispositivo</option>{(inventory??[]).map(unit=><option value={unit.id} key={unit.id}>{unit.imei_1} · {relatedName(unit.product_brands)} {relatedName(unit.product_models)} · L {unit.cash_price}</option>)}</select></div><div className="field"><label htmlFor="requested_price">Precio financiado</label><input id="requested_price" name="requested_price" type="number" min="1" step="0.01" required/></div><div className="field"><label htmlFor="down_payment">Prima propuesta</label><input id="down_payment" name="down_payment" type="number" min="0" step="0.01" required/></div><div className="field"><label htmlFor="term">Plazo</label><select id="term" name="term" required><option value="">Selecciona el plazo</option>{[3,6,9,12,18,24].map(term=><option value={term} key={term}>{term} meses</option>)}</select></div><div className="form-actions"><button className="button" type="submit">Enviar a análisis</button></div></div></div></form></section></AppShell>}
