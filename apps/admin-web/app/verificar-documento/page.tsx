import { createClient } from "@supabase/supabase-js";
type Verification={valid:boolean;contract_number:string;accepted_at:string;bundle_generated_at:string;organization:string;evidence_hash:string};
export default async function VerifyDocument({searchParams}:{readonly searchParams:Promise<{codigo?:string}>}){const{codigo}=await searchParams;let result:Verification|null=null;if(codigo&&process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY){const client=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,{auth:{persistSession:false}});const{data}=await client.rpc("verify_credit_document",{p_verification_code:codigo});result=data as Verification|null}return <main className="verification-page"><section className="verification-card"><div className="logo">Credi<span>Cel</span></div>{result?.valid?<><span className="badge success">Documento auténtico</span><h1>{result.contract_number}</h1><p>Este código corresponde a un paquete firmado y conservado por <strong>{result.organization}</strong>.</p><dl><div><dt>Firma registrada</dt><dd>{new Intl.DateTimeFormat("es-HN",{dateStyle:"long",timeStyle:"short",timeZone:"America/Tegucigalpa"}).format(new Date(result.accepted_at))}</dd></div><div><dt>Huella digital</dt><dd>{result.evidence_hash}</dd></div></dl><small>La verificación confirma integridad y registro técnico; no sustituye asesoría jurídica.</small></>:<><span className="badge danger">No verificado</span><h1>Código no válido</h1><p>Revisa el enlace o solicita una copia nueva a la tienda.</p></>}</section></main>}
