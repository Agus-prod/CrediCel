import Link from "next/link";
import {registerPlatformOwner} from "./actions";
export default async function PlatformRegistration({searchParams}:{readonly searchParams:Promise<{error?:string;check_email?:string}>}){const q=await searchParams;return <main className="auth-shell"><section className="auth-card platform-register-card"><div className="logo">Credi<span>Cel</span></div><span className="eyebrow">Propietario de la plataforma</span><h1>Crea tu cuenta maestra</h1><p className="muted">Esta cuenta administra organizaciones, planes y pagos. No crea una tienda ni inicia una prueba.</p>{q.error&&<div className="error">No se pudo crear la cuenta. Revisa los datos o inicia sesión si ya existe.</div>}{q.check_email&&<div className="notice">Te enviamos la confirmación a augustocolindres1@gmail.com. Confirma el correo para entrar al panel maestro.</div>}{!q.check_email&&<form action={registerPlatformOwner} className="form"><div className="field"><label htmlFor="full_name">Tu nombre</label><input id="full_name" name="full_name" defaultValue="Augusto Colindres" required/></div><div className="field"><label htmlFor="email">Correo maestro</label><input id="email" name="email" type="email" value="augustocolindres1@gmail.com" readOnly/></div><div className="field full-field"><label htmlFor="password">Crea una contraseña</label><input id="password" name="password" type="password" minLength={10} autoComplete="new-password" required/><small>Mínimo 10 caracteres.</small></div><div className="form-actions full-field"><button className="button">Crear cuenta maestra</button></div></form>}<Link className="auth-back" href="/login">Volver a iniciar sesión</Link></section></main>}
