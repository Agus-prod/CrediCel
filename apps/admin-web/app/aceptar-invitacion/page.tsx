import{createServerSupabase}from"@/lib/supabase/server";import{acceptInvitation}from"./actions";
export default async function AcceptInvite({searchParams}:{readonly searchParams:Promise<{token?:string;error?:string;sent?:string}>}){const q=await searchParams;const s=await createServerSupabase();const{data:{user}}=await s.auth.getUser();return <main className="login"><div className="login-orb one"/><div className="login-orb two"/><section className="login-card"><div className="logo">Credi<span>Cel</span></div><h1>Únete al equipo</h1><p className="muted">Activa tu acceso seguro a la organización y tienda asignadas.</p>{q.error&&<div className="error">{q.error}</div>}{q.sent&&<div className="notice">Revisa tu correo, confirma la cuenta y vuelve a abrir la invitación.</div>}<form action={acceptInvitation}><input name="token"type="hidden"value={q.token??""}/>{!user&&<><div className="field"><label htmlFor="email">Correo invitado</label><input id="email"name="email"type="email"required/></div><div className="field"><label htmlFor="password">Crea una contraseña</label><input id="password"name="password"type="password"minLength={8}required/></div></>}<button className="button wide"type="submit">{user?"Aceptar invitación":"Crear cuenta y continuar"}</button></form></section></main>}
