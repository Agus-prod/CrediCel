const required=[{type:"dni_front",label:"DNI frontal"},{type:"dni_back",label:"DNI posterior"},{type:"selfie",label:"Selfie de verificación"},{type:"address_proof",label:"Comprobante de domicilio"}]as const;
export function DocumentChecklist({documents}:{readonly documents:readonly{readonly document_type:string}[]}){const present=new Set(documents.map(document=>document.document_type));return <div className="document-review"><strong>Verificación documental</strong><div>{required.map(document=><span className={present.has(document.type)?"document-ok":"document-missing"}key={document.type}>{present.has(document.type)?"✓":"!"} {document.label} · {present.has(document.type)?"Recibido":"Falta"}</span>)}</div></div>}
