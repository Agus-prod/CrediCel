import{AppShell}from"@/components/app-shell";import{createServerSupabase}from"@/lib/supabase/server";import{addBranch}from"./actions";
export default async function Organization({searchParams}:{readonly searchParams:Promise<{error?:string;created?:string}>}){const q=await searchParams;const s=await createServerSupabase();const{data}=await s.from("branches").select("id,name,code,address,phone,status").order("name");return <AppShell><section className="section"><div className="toolbar"><div><div className="eyebrow">Organización</div><h1>Tiendas</h1><p className="muted">Cada tienda mantiene su equipo, inventario, solicitudes y caja dentro de la misma empresa.</p></div></div>{q.error&&<div className="error">{q.error}</div>}{q.created&&<div className="notice">Tienda creada correctamente.</div>}<div className="grid branch-grid">{(data??[]).map(b=><article className="card"key={b.id}><span className={`badge ${b.status==="active"?"success":"danger"}`}>{b.status==="active"?"Activa":"Inactiva"}</span><h2>{b.name}</h2><p className="muted">Código {b.code}</p><p>{b.address}</p><small>{b.phone||"Sin teléfono"}</small></article>)}</div><div className="card form-card section"><div className="form-title"><div><h2>Nueva tienda</h2><p className="muted">El límite depende del plan contratado.</p></div></div><form action={addBranch}className="form"><div className="field"><label htmlFor="name">Nombre</label><input id="name"name="name"required/></div><div className="field"><label htmlFor="code">Código</label><input id="code"name="code"required/></div><div className="field"><label htmlFor="address">Dirección</label><input id="address"name="address"required/></div><div className="field"><label htmlFor="phone">Teléfono</label><input id="phone"name="phone"type="tel"/></div><div className="form-actions"><button className="button"type="submit">Crear tienda</button></div></form></div></section></AppShell>}
