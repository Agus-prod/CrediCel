import {updatePassword} from "./actions";export default async function NewPassword({searchParams}:{readonly searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="login"><form action={updatePassword} className="login-card"><div className="logo">Credi<span>Cel</span></div><h1>Nueva contraseña</h1><p className="muted">Utiliza al menos 10 caracteres.</p>{error&&<p className="error">Las contraseñas no coinciden o el enlace ya expiró.</p>}<div className="field"><label htmlFor="password">Nueva contraseña</label><input id="password" name="password" type="password" minLength={10} required/></div><div className="field"><label htmlFor="confirmation">Confirmar contraseña</label><input id="confirmation" name="confirmation" type="password" minLength={10} required/></div><button className="button wide">Guardar contraseña</button></form></main>}
