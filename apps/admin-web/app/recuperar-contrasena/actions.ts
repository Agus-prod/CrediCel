"use server";import {createServerSupabase} from "@/lib/supabase/server";import {redirect} from "next/navigation";export async function requestReset(formData:FormData){const supabase=await createServerSupabase();await supabase.auth.resetPasswordForEmail(String(formData.get("email")??""),{redirectTo:"http://localhost:3001/nueva-contrasena"});redirect("/recuperar-contrasena?sent=1")}
