import Link from "next/link";import {requestReset} from "./actions";export default async function Reset({searchParams}:{readonly searchParams:Promise<{sent?:string}>}){const {sent}=await searchParams;return <main className="login"><form action={requestReset} className="login-card"><div className="logo">Credi<span>Cel</span></div><h1>Recuperar contraseña</h1><p className="muted">Te enviaremos un enlace seguro.</p>{sent&&<p className="success">Si el correo está registrado, recibirás las instrucciones.</p>}<div className="field"><label htmlFor="email">Correo</label><input id="email" name="email" type="email" required/></div><button className="button wide">Enviar enlace</button><Link className="register-link" href="/login">Volver al acceso</Link></form></main>}
