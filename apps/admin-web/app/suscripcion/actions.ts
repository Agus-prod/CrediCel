"use server";import{redirect}from"next/navigation";import{createServerSupabase}from"@/lib/supabase/server";export async function changePlan(formData:FormData){const supabase=await createServerSupabase();const{error}=await supabase.rpc("change_subscription_plan",{p_plan_code:String(formData.get("plan")??"")});if(error)redirect(`/suscripcion?error=${encodeURIComponent(error.message)}`);redirect("/suscripcion?updated=1")}
