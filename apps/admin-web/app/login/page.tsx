import {login} from "./actions";
import Link from "next/link";
export default async function Login({searchParams}:{readonly searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="login"><div className="login-orb one"/><div className="login-orb two"/><form action={login} className="login-card"><div className="logo">Credi<span>Cel</span></div><h1>Bienvenido</h1><p className="muted">Accede al centro de operaciones.</p>{error&&<p className="error">Correo o contraseña incorrectos.</p>}<div className="field"><label htmlFor="email">Correo</label><input id="email" name="email" type="email" placeholder="nombre@credicel.hn" required/></div><div className="field"><label htmlFor="password">Contraseña</label><input id="password" name="password" type="password" required/></div><button className="button wide" type="submit">Ingresar</button><div className="login-divider"><span>o</span></div><Link className="register-link" href="/registro-organizacion">Crear una organización</Link><p className="login-help">Acceso exclusivo para personal autorizado</p></form></main>}
